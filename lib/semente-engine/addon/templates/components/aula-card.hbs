
{{#if (or (equalstr situacao selectedSituacao) (equalstr selectedSituacao ""))}}
<div
  class="media {{if (not aula.hasContent) 'media--card media--card--is-disabled' 'media--card  media--card--fundo'}}">
<div class="degrade"></div>
  <!--  <picture class="media__picture">
      <img class="media__thumb" id="thumbnail-aula-card{{aula.id}}" src="{{aula.thumbnail}}" alt="{{aula.titulo}}"/>
    </picture> -->
  {{aulaThumb}}
  <div class="media__body flow-md self-stretch">
    <div class="titulo-aula">
      <div class="flow-xxs">
        {{#if aula.unidade}}
        <span class="title-aux">
          {{aula.unidade.titulo}}
        </span>
        {{/if}}
        
        <span class="media__title  media__title--novo-titulo">{{aula.titulo}}</span>
      </div>
    </div>
    <div class="descricao-aula">
      <p class="media__title--nova-descricao-aula">Descrição do bloco de no máximo 2 linhas.</p>
    </div>

    <div class="habilidades">
      <span class="habilidades etiquetas">AS</span>
      <span class="habilidades etiquetas">FO</span>
      <span class="habilidade etiquetas">ES</span>
      <span class="habilidades etiquetas">CD</span>
      <span class="habilidade etiquetas">EP</span>
      <span class="habilidades etiquetas">MT</span>
      <span class="habilidades etiquetas">RT</span>
      <span class="habilidades etiquetas">CF</span> 
      <span class="habilidades etiquetas">DE</span> 
      <span class="habilidades etiquetas">PE</span>
    </div>
    <!--   <span class="flex flex-row items-center m-bottom--md">
        <svg xmlns="http://www.w3.org/2000/svg"
            class="stroke-green-20 m-right--xs"
            fill="none"
            height="24"
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            stroke="currentColor"
            viewBox="0 0 24 24"
            width="24">
          <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"></path>
          <path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"></path>
        </svg>
       {{#if (equalstr situacao "Aplicada")}}
          <span>Aplicada em: <span class="fw--4">{{UltimaAplicacao.dataAplicacaoFormatShort}}</span></span>
        {{else}}
          <span>{{situacao}}</span>
        {{/if}} 
      </span>-->
    <div class="btn-materiais-aula">
      {{#if (not aula.hasContent)}}
      <div class="btn btn--disabled btn--ghost btn--xxs novo-btn-materiais-aula">
        <span class="icon-lock m-right--xs"></span>
        <span>Ainda não há materiais</span>
      </div>
      {{else}}
      {{#link-to 'aulas.content' aula.id class="btn btn--ghost btn--xxs btn-materiais-aula"}}
      <span class="btn__label">Ver materiais</span>
      {{/link-to}}
      {{/if}}
    </div>
  </div>

</div>
{{/if}}